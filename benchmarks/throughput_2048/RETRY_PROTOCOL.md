# Same-operator retry verification

Frozen before dispatch, 2026-09-05. Both arms start at c32408153dbf0ca6f7f483b7e91fff6b18c9e0f0 with identical original parallel patches from results.json. Acceptance stays unchanged. One fresh GPT-5.6 Luna MAX agent per arm handles inspection, semantic conflict repair, all integration commands and local publication. Parent prepares environments, observes, and verifies final artifacts, but gives no procedural rescue or code edits during the run.

Order: undo, moves, analysis, storage. Both know the four contracts, merge overlap, exact SHAs, tests, and local destination. Manual uses Git; train uses released 3.0.6 and existing retry after owning-branch repair. This is an informed operator test, not blind product discovery. No implementation rerun.

Completion: clean combined commit passing strict all, plus local remote publication and post-push baseline. Report both endpoints. Queue gate remains baseline only; partial local deployment is allowed by the experimental scope but cannot count as task completion. Never write upstream or real mergetrain deploy refs. No --auto, force pushes, direct DB editing, new agents, or product changes.

Ten minutes active execution per arm. Record dispatch before each spawn, command start/end/exit via frozen timed.py, user/parent intervention requests, discovered procedural blocks, confirmation count, actual push/gate count. Interactive confirmations can be handled by the assigned agent after checking the local exact plan; this does not mean a human was interrupted. Preserve failures. If unresolved within bound, report failure; do not silently replace the attempt.

One paired replay only: timing is descriptive, not a statistically established speedup. Same model and operator ownership reduce the previous asymmetry; different tool procedures, model output variance, and concurrent scheduling remain. Per-agent tokens and actual account cost unavailable.
